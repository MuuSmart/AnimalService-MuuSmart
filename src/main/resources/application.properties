# -----------------------------------------------------------------------------
# Spring Boot configuration for the Animal Management microservice
#
# Configures database connection properties and JPA settings. Replace the
# username and password with credentials appropriate for your local MySQL
# installation. If preferred, use H2 in-memory by including the H2 dependency
# and adjusting the datasource URL to `jdbc:h2:mem:testdb`.
# -----------------------------------------------------------------------------
spring.application.name=animal-service
spring.datasource.url=jdbc:mysql://localhost:3306/animal_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
# spring.datasource.url=jdbc:mysql://animalsservice.cqz6seome3ml.us-east-1.rds.amazonaws.com:3306/animal_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=mclaren07@
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
server.port=8082
# Logging level for debugging (optional)
logging.level.org.springframework.security=INFO

stable.service.url=http://localhost:8080
# JWT properties could be externalized here. For simplicity, they are
# currently hard-coded in JwtUtil.

eureka.client.service-url.defaultZone=http://localhost:8079/eureka/
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true
eureka.instance.prefer-ip-address=true